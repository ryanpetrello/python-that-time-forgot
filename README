Assuming you're in the same directory as this README (and the Vagrantfile):

1.  Install Virtualbox (https://www.virtualbox.org)
2.  Install Vagrant (https://www.vagrantup.com)
3.  $ pip install ansible
4.  $ vagrant up

Tinker w/ Apache + CGI or FastCGI:

Python 1.2: $ curl -v http://localhost:8080/cgi-bin/helloworld.cgi
Python 1.4: $ curl -v http://localhost:8080/cgi-bin/helloworld.fcgi

SSH and play with Python 1.2 and 1.4:
$ vagrant ssh

Python 1.2: $ python
Python 1.4: $ python1.4

Python 1.5:
$ python1.5
>>> from Mailman import Message
>>> dir(Message)
